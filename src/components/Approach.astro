---
const steps = [
    {
        step: '01',
        title: 'Brief & cadrage',
        description:
            "Vous me partagez le lieu, la date souhaitée et le rendu attendu. Je vérifie la zone (NOTAM, restrictions) et propose un plan de vol simple.",
        metrics: ['Validation DGAC', 'Repérage carte', 'Brief clair']
    },
    {
        step: '02',
        title: 'Vol assisté',
        description:
            "Je réalise la mission avec mon drone 4K stabilisé, en vous montrant des prévisualisations sur place pour valider les angles clés.",
        metrics: ['Télépilote DGAC', 'Sécurité S1/S3', 'Matériel 4K']
    },
    {
        step: '03',
        title: 'Post-production',
        description:
            "Je trie les images, applique une colorimétrie sobre et vous livre un dossier partagé avec photos HD et vidéos compressées pour le web.",
        metrics: ['Livraison 72h', 'Lien cloud', 'Exports HD & web']
    }
];
---
<section id="approach" class="relative flex min-h-screen items-center bg-black py-32 text-white">
    <div class="absolute inset-0 -z-20 bg-gradient-to-b from-black via-slate-950 to-black"></div>
    <div class="absolute inset-y-0 left-1/2 -z-10 hidden w-px bg-gradient-to-b from-transparent via-purple-500/40 to-transparent md:block"></div>
    <div class="container mx-auto max-w-5xl px-6">
        <div class="mb-16 text-center">
            <span class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-4 py-2 text-xs font-semibold uppercase tracking-[0.4em] text-purple-200">Workflow</span>
            <h2 class="mt-6 text-4xl font-bold text-white md:text-5xl">Un déroulé simple et transparent</h2>
            <p class="mt-4 text-base leading-relaxed text-gray-300 md:text-lg">
                Du premier échange à la livraison, je reste votre seul interlocuteur. Chaque étape est documentée pour que vous sachiez où en est la mission.
            </p>
        </div>
        <div class="space-y-10">
            {steps.map(({ step, title, description, metrics }) => (
                <article class="relative overflow-hidden rounded-3xl border border-white/10 bg-white/5 p-8 shadow-lg shadow-purple-500/10 backdrop-blur-xl md:grid md:grid-cols-[auto_1fr] md:items-center md:gap-10">
                    <div class="flex items-center gap-4 md:flex-col md:items-start">
                        <span class="text-4xl font-black text-transparent bg-clip-text bg-gradient-to-br from-purple-400 via-fuchsia-400 to-indigo-400">{step}</span>
                        <div class="h-10 w-px bg-gradient-to-b from-purple-400/40 to-transparent md:h-20 md:w-px"></div>
                    </div>
                    <div class="space-y-5">
                        <h3 class="text-2xl font-semibold text-white">{title}</h3>
                        <p class="text-sm leading-relaxed text-gray-300 md:text-base">{description}</p>
                        <div class="flex flex-wrap gap-3 text-xs uppercase tracking-[0.3em] text-gray-400">
                            {metrics.map((item) => (
                                <span class="rounded-full border border-white/10 px-3 py-1">{item}</span>
                            ))}
                        </div>
                    </div>
                </article>
            ))}
        </div>
    </div>
</section>
