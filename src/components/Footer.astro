---
const pathname = Astro.url.pathname;
const linkTo = (section: string) => (pathname === '/' ? `#${section}` : `/#${section}`);
---

<footer class="bg-black py-12 text-white">
    <div class="container mx-auto max-w-5xl px-6">
        <div class="flex flex-col gap-8 rounded-3xl border border-white/10 bg-white/5 p-8 text-sm text-gray-300 backdrop-blur-xl md:flex-row md:items-center md:justify-between">
            <div>
                <p class="text-xs uppercase tracking-[0.4em] text-purple-200">Stellaris Drone</p>
                <h3 class="mt-3 text-2xl font-semibold text-white">Imaginer, planifier, capturer.</h3>
            </div>
            <div class="flex flex-wrap gap-3 text-xs uppercase tracking-[0.35em]">
                <a href={linkTo('services')} class="rounded-full border border-white/15 px-4 py-2 transition hover:border-white/40">Services</a>
                <a href={linkTo('approach')} class="rounded-full border border-white/15 px-4 py-2 transition hover:border-white/40">Approche</a>
                <a href={linkTo('contact')} class="rounded-full border border-white/15 px-4 py-2 transition hover:border-white/40">Contact</a>
            </div>
            <a href="mailto:contact@stellarisdrone.com" class="rounded-full bg-gradient-to-r from-purple-500 via-fuchsia-500 to-indigo-500 px-5 py-2 font-semibold text-white shadow-lg shadow-purple-500/20">Brief rapide</a>
        </div>
        <div class="mt-6 flex flex-col items-start justify-between gap-4 text-xs text-gray-500 md:flex-row">
            <p>&copy; {new Date().getFullYear()} Stellaris Drone. Auditer le ciel, protéger vos projets.</p>
            <div class="flex gap-4">
                <a href="/mentions-legales" class="transition hover:text-gray-300">Mentions légales</a>
                <a href="/politique-confidentialite" class="transition hover:text-gray-300">Politique de confidentialité</a>
            </div>
        </div>
    </div>
</footer>
